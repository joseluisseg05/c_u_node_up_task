doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePag
            p Coloca tu nueva constraseña

            form.caja-login(method="POST")
                .campo  
                    label(for="password") Password:  
                    input(type="password" name="password" placeholder="Nueva contraseña" value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer contraseña")